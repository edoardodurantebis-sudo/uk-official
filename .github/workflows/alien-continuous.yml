name: GB Alien 1s Watcher — PUBLIC ARCHIVE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Public scheduled watcher disabled. Canonical UK engine moved to private repository.'
